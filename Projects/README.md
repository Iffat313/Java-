1) Project 1:
   * SwimSchedule:
     - Allow user to see schedules of both swim instructors or either schedule
     - Allow user to book lesson with either swim instructor
     - Allow user to omit reservation
     - Allow user to book group lessons
   * MorseCode:
     - Allow user to enter input for String and conver the string to a MorseCode (One space between each letter for a word, and three spaces between each word)
     - Allow user to enter input String of a morse code and convert it to a regular String
    
